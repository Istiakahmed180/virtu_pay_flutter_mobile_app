import 'package:flutter/material.dart';

class AppColors {
  // ------------------ LIGHT THEME ------------------
  // Background Colors
  static const Color background = Color(0xFFE6EAFD);

  // Primary Colors
  static const Color primary = Color(0xFFD3F570);

  // Secondary Colors
  static const Color secondary = Color(0xFF7E52FF);
  static const Color secondaryTwo = Color(0xFFE3D1FF);

  // Text Colors
  static const Color textPrimary = Color(0xFF0D0E0F);
  static const Color textTertiary = Color(0xFF030306);

  // Border Colors
  static const Color border = Color(0xFF1A1A1A);

  // Error/Warning/Success

  // Utility
  static const Color white = Color(0xFFFFFFFF);
  static const Color grey = Color(0xFF6C7278);
  static const Color transparent = Colors.transparent;
}
